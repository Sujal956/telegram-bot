services:
  - type: worker
    name: telegram-bot
    runtime: python
    plan: free

    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py

    autoDeploy: true

    envVars:
      - key: BOT_TOKEN
        sync: false

      - key: ADMIN_CHAT_ID
        value: "8842963150"

      - key: PRIVATE_GROUP_LINK
        value: "https://t.me/+dE0mrYkSOG0yZTE1"
